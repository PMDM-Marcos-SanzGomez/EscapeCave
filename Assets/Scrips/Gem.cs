using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Gem : MonoBehaviour
{
    public static int numGems = 0;
    // Start is called before the first frame update
    void Start()
    {
        numGems++;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
